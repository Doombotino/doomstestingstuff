




















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































document.addEventListener("DOMContentLoaded", function() {


    // Autoplay Google Slides
    let slidesFrame = document.getElementById("slides-frame");
    if (slidesFrame) {
        slidesFrame.src = slidesFrame.src.replace("start=false", "start=true");
    } else {
        console.error("Slides iframe not found!");
    }
});



document.addEventListener("DOMContentLoaded", function() {
    // Select the YouTube iframe
    let youtubeFrame = document.querySelector("iframe[src*='youtube.com/embed']");

    if (youtubeFrame) {
        setTimeout(() => {
            console.log("Unmuting video...");
            let currentSrc = youtubeFrame.src;
            let newSrc = currentSrc.replace("mute=1", "mute=0");
            youtubeFrame.src = newSrc; // Reload iframe with unmuted video
        }, 8000); // 8-second delay
    } else {
        console.error("YouTube iframe not found!");
    }

    // Autoplay Google Slides
    let slidesFrame = document.getElementById("slides-frame");
    if (slidesFrame) {
        slidesFrame.src = slidesFrame.src.replace("start=false", "start=true");
    } else {
        console.error("Slides iframe not found!");
    }
});
